# Runtime dependencies for nextdoor-watcher.
# Playwright is pinned; run `playwright install chromium` after installing.
click==8.1.7
playwright==1.44.0
beautifulsoup4==4.12.3
tomli==2.0.1 ; python_version < "3.11"

# Test-only (install with: pip install -r requirements.txt pytest)
pytest==8.2.0
